# Vibecode website

Static landing page ready for any free static hosting service. Upload the contents of this folder as the site root.

Before publishing, replace `support@example.com` in `index.html` and add the actual client download URL in `script.js` (the current button opens a release placeholder).
